import random
import numpy as np


def posMatrix(mat):
  rows = len(mat1)
  cols = len(mat1[0])
  mOut = np.empty((rows, cols), dtype = 'int')


def printMatrix(mat):
  for row in mat:
    for num in row:
      print(f"{num:3d}" , end = "")
    print()


def main():
  pass


if __name__ == "__main__":
  main() 